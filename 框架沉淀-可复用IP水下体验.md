# 框架沉淀：可复用 IP 水下体验

## 一句话定义

这套框架不是单一的《庆余年》Demo，而是一套可迁移到不同长视频 IP 的 **AI 原生衍生体验框架**。

它的核心机制是：

> 固定主线不被改写，用户通过工具选择“往哪里看得更深”，AI 只负责在既定剧情节点上判断并生成水下纵深。

换句话说，框架负责体验机制，IP 内容包负责具体世界。

---

## 框架固定部分

### 1. 视觉小说式播放引擎

固定能力：

- 背景画面固定，随着剧情节点切换
- 人物对话时展示立绘与对话框
- 镜头/旁白、人物对话、用户感知、水下回响使用不同样式
- 支持自动播放和手动推进
- 工具返回后以半透明"水下浮起"层播放结构化小片段，播完自动回到当前主线幕
- 结尾生成本次体验总结、分享卡片和旅程回看卡组

这部分不依赖具体 IP，可以复用。

### 2. 固定主线 + AI 打光

框架原则：

- 主线剧情由内容包预先定义
- AI 不负责生成或改变主线
- AI 只在用户点击工具后判断：当前身份、当前场景、当前工具，是否能看到更深一层的信息
- 默认没有发现，只有存在明确触发物时才返回有价值内容

这避免产品变成普通 AI 互动小说，也保护原作作者性。

### 3. 工具体系

当前工具：

| 工具 | 含义 | 通用能力 |
| --- | --- | --- |
| 侧耳 | 听见低语、潜台词、信息差 | 听觉纵深 |
| 追忆 | 连接前因、旧事、历史线索 | 时间纵深 |
| 细看 | 观察物件、表情、动作异常 | 视觉纵深 |
| 跟随 | 跟着某人离开当前视角 | 空间纵深 |

工具可以常驻界面。工具命名可随 IP 调整，例如科幻 IP 可改为“监听 / 回溯 / 扫描 / 追踪”。是否提示推荐工具要谨慎处理：推荐态不应暗示“一定有内容”，否则会削弱 AI 自主判断的可信度。

### 4. AI 调用与输出协议

固定输出结构：

```json
{
  "hasDepth": true,
  "content": "纵深内容摘要",
  "depthTag": "纵深标签",
  "beats": [
    {
      "kind": "narration | dialogue | perception | thought",
      "speaker": "可选，说话者或感知主体",
      "mood": "可选，语气",
      "text": "单屏可读的小片段"
    }
  ],
  "innerThoughts": [
    {
      "character": "角色名",
      "thought": "角色内心活动"
    }
  ]
}
```

固定判断规则：

- 当前场景必须有明确对象
- 用户身份必须能自然接触到该信息
- 工具必须与对象匹配
- 有发现时拆成 2-4 个 beat，每个 beat 控制在单屏可读范围
- 不允许泄露小人物不可能知道的上帝视角信息
- 不允许改写主线结局

---

## IP 内容包需要替换的部分

### 1. IP 基础配置

用于定义产品名、视觉主题和入口文案。

示例：

```ts
export const ipConfig = {
  id: 'joy-of-life',
  name: '庆余年',
  productName: '庆余年 · 水下',
  tagline: '每部剧都是一座冰山。你看到的，只是水面以上。',
};
```

### 2. 世界观设定

需要提供：

- 世界背景
- 权力结构 / 组织结构
- 关键地点
- 关键事件
- 社会规则
- 禁止 AI 触碰的设定边界

在当前项目里，对应 `worldbuilding.ts` 和 `prompts.ts` 里的世界观 brief。

### 3. 核心角色档案

每个角色至少包括：

```ts
{
  name: '范闲',
  title: '范府少爷',
  personality: '外表随和，内心敏锐，重情重义',
  motivation: '调查母亲真相，保护身边的人',
  avatar: '/pixels/fan-xian.png'
}
```

角色档案用于：

- 对话立绘映射
- AI 判断角色动机
- AI 生成潜台词或内心活动

### 4. 用户身份设定

用户不是主角，而是一个能照见主线边缘的小人物。

每个身份需要定义：

```ts
{
  id: 'teahouse',
  name: '周福生',
  title: '京都茶馆掌柜',
  location: '牛栏街·听风茶馆',
  description: '经营茶馆二十余年的老掌柜',
  depthAngle: '朝堂风云的民间回响',
  avatar: '/pixels/zhou-fusheng.png'
}
```

关键是写清楚：

- 他是谁
- 他在哪里
- 他能看到什么
- 他不能知道什么
- 他和主线事件的距离

### 5. 固定故事主线

每条身份线由若干 `StoryNode` 组成：

```ts
{
  id: 'teahouse_01',
  title: '清晨开门',
  sceneDescription: '场景/镜头描述',
  dialogues: [
    {
      character: '老李',
      mood: '随意',
      spoken: '福生，今儿水烧得早啊。'
    }
  ],
  perception: '你作为这个身份的感知与动作',
  sceneTag: '日常'
}
```

框架会把一个节点拆成：

- 镜头/旁白
- 人物对话
- 你的感知
- 水下回响

因此内容包只需要提供结构化文本，不需要关心前端渲染细节。

### 6. 素材清单

至少需要：

- 用户身份头像/立绘
- 核心角色头像/立绘
- 通用 NPC 素材
- 每个关键剧情阶段的背景图

推荐目录结构：

```text
public/assets/{ip-id}/
  characters/
  backgrounds/
  npcs/
```

当前项目为了快速 Demo，暂时放在：

```text
underwater/public/pixels/
```

### 7. 资源映射

需要定义：

- 哪个角色名对应哪张立绘
- 哪个故事节点对应哪张背景
- 跑龙套角色使用哪些通用 NPC 素材

当前项目对应 `assets.ts`。

---

## 换一个 IP 的流程

1. 选定 IP 和主线事件
   - 不建议覆盖整部剧
   - 只选一个高密度事件，例如刺杀、宴会、审判、告别、事故、庆典

2. 设计 2-3 个小人物身份
   - 每个身份都应站在主线边缘
   - 身份之间最好看到不同层面的“水下”

3. 写固定故事节点
   - 每条身份线 5-8 幕即可
   - 每幕控制在一个明确场景和一个情绪推进

4. 准备角色和背景素材
   - 先保证主录屏路径完整
   - 再补其他身份和支线

5. 替换世界观与角色 prompt
   - 保留 AI 判断协议
   - 替换世界规则、角色动机、身份权限

6. 调整工具命名和风格
   - 古装：侧耳 / 追忆 / 细看 / 跟随
   - 科幻：监听 / 回溯 / 扫描 / 追踪
   - 悬疑：窃听 / 复盘 / 检视 / 尾随

7. 测试 AI 返回频率
   - 如果太容易返回有发现，收紧 prompt
   - 如果太少返回，补充场景中的触发物

---

## 对腾讯视频的复用价值

这套框架的价值不在于只做一个《庆余年》衍生 Demo，而在于它可以变成腾讯视频 IP 的通用衍生层。

可迁移方向：

- 古装权谋剧：从仆役、店家、文书视角看朝堂暗线
- 悬疑剧：从邻居、值班员、记者视角看案件边缘线索
- 科幻剧：从普通工程师、观测员、档案员视角看宏大事件
- 都市剧：从同事、服务员、物业、司机视角看人物关系暗流
- 综艺：从导演组、后期剪辑、工作人员视角看节目素材的另一条叙事线

商业价值：

- 延长 IP 生命周期
- 承接剧集播完后的深度消费需求
- 把站外讨论、考据、二创兴趣拉回腾讯视频站内
- 形成可分享的个性化体验
- 通过同一框架低成本扩展多个 IP

---

## 当前项目中的固定与替换总结

| 类型 | 当前文件 | 是否复用 | 说明 |
| --- | --- | --- | --- |
| 播放页 | `src/app/play/page.tsx` | 可复用 | 视觉小说播放、自动推进、工具调用 |
| 渲染组件 | `src/components/*` | 可复用 | 旁白、对话、感知、回响、工具栏 |
| AI 调用 | `src/lib/ai.ts` | 可复用 | OpenAI 兼容 API 调用 |
| Prompt 模板 | `src/lib/prompts.ts` | 半复用 | 结构复用，世界观与身份内容替换 |
| 世界观与角色 | `src/lib/worldbuilding.ts` | 需替换 | IP 内容包核心 |
| 主线剧情 | `src/lib/storyNodes.ts` | 需替换 | 每个 IP / 每个事件重写 |
| 资源映射 | `src/lib/assets.ts` | 半复用 | 结构复用，路径和角色名替换 |
| 图片素材 | `public/pixels/*` | 需替换 | 每个 IP 独立素材包 |

---

## 后续可演进方向

短期：

- 把 `worldbuilding.ts / storyNodes.ts / assets.ts` 收拢成 `content/joy-of-life/`
- 增加 `ipConfig.ts`
- 将工具名称与 UI 文案改为可配置

中期：

- 支持多个 IP 内容包切换
- 支持同一 IP 多个事件包
- 增加内容包校验工具，检查角色头像、背景图、storyNode 是否缺失

长期：

- 接入腾讯视频真实剧集页
- 在剧集播完后推荐“进入水下”
- 将用户体验生成可分享卡片
- 支持官方运营配置不同角色视角和限定事件
